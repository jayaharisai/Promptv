[project]
name = "promptv-sdk"
version = "0.1.0"
description = "Python client for retrieving active Promptv prompts"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/promptv"]
